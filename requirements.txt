# Aegis Core Dependencies

# Table formatting for CLI output
tabulate>=0.9.0

# Config file parsing
PyYAML>=6.0

# System monitoring (Day 6)
psutil>=5.9.0

# Windows GUI automation (optional - only needed for active window detection)
pywin32>=306; sys_platform == 'win32'

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Future dependencies (for later phases)
# pyautogui>=0.9.54  # Desktop automation (Phase C)
# pywinauto>=0.6.8   # Windows automation (Phase C)
